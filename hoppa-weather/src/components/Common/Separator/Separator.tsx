// src/components/Separator.tsx
import React from 'react';
import { View } from 'react-native';

const Separator = () => (
	<View
		style={{ height: 1, borderBottomWidth: 1, borderBottomColor: 'black' }}
	/>
);

export default Separator;
